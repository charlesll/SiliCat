

# import data
#xinput = np.loadtxt('../data/Inputs_test.txt',delimiter='\t')
#yinput = np.loadtxt('../data/Outputs_test.txt',delimiter='\t')

#Giorda_visc, Giorda_Tg, Giorda_B, Giorda_C = giordano(xinput)

#MSE = np.sqrt(1/(float(len(xinput)))*sum((Giorda_visc[:]-yinput[:])**2))
